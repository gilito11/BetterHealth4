name: Generar Reporte de Issues

on:
  schedule:
    - cron: "0 0 * * 1"  # Se ejecuta cada lunes a las 00:00 UTC
  workflow_dispatch:  # Permite ejecución manual

permissions:
  contents: write  # Permiso para hacer commit en el repositorio

jobs:
  generate-report:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Clonar el repositorio
        uses: actions/checkout@v4

      - name: 🐍 Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: 📦 Instalar dependencias
        run: pip install requests matplotlib

      - name: 📊 Ejecutar el script de generación de gráficos
        run: python script.py  # Asegúrate de que el nombre coincide con tu script

      - name: 📂 Mover gráficos a la carpeta reports/
        run: |
          mkdir -p reports
          mv grafico_semanal.png grafico_global_mensual.png grafico_global_semanal.png reports/

      - name: 📝 Hacer commit y push de los gráficos
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git add reports/
          git commit -m "📊 Actualizar gráficos de issues - Semana $(date +'%V %B')"
          git push
        continue-on-error: true  # Evita errores si no hay cambios nuevos

      - name: 📤 Subir gráficos como artifacts
        uses: actions/upload-artifact@v4
        with:
          name: report-images
          path: reports/
